**Ghidra Decompile:**
```
${decompiled}
```

**Struct/type context, cross-references, source context, and reference examples
are appended below by the orchestrator (stable context first).**

Reverse the above function into clean C++23.

**Target:** ${class_name}::${function_name} at ${address}
